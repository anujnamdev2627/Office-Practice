import { HoverapiDirective } from './hoverapi.directive';

describe('HoverapiDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverapiDirective();
    expect(directive).toBeTruthy();
  });
});
